1. git init
2. git add .
3. git commit -m "message"
4. git remote add origin <https://github.com/Kethanareddy71/JFS_VISEM>

Example:
git remote add origin https://github.com/Kethanareddy71/JFS_VISEM

5. git push origin main
6. you can check in remote repository
----------------------------------------
If you can add any files in local repository or you can change the existing files
in local repository

1. git status
2. git add .
3. git commit -m "message"
4, git push origin main

----------------------
If you want to retri
git clone <https://github.com/Kethanareddy71/JFS_VISEM>